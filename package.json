{
  "name": "design-token-figma",
  "version": "1.0.0",
  "description": "Ejemplo práctico de como implementar los design token utilizando la API de figma",
  "main": "index.js",
  "scripts": {
    "build": "node index.js && node build.js",
    "builds": "style-dictionary build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "design token",
    "figma"
  ],
  "author": "Javier Samper Arias",
  "license": "ISC",
  "devDependencies": {
    "cpx": "^1.5.0",
    "node-fetch": "^2.6.0",
    "style-dictionary": "^2.10.2"
  }
}
